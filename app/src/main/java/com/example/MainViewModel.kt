package com.example

import androidx.lifecycle.ViewModel

class MainViewModel : ViewModel() {
    // ViewModel to handle authentication state and initial logic
    // Currently empty as it's the start screen, but prepared for Firebase Auth integration.
}
